# Reading report intent

A reading has a name, numeric value, unit, and an `included` flag. Formatting
uses `Name: value unit`. Reports preserve input order, include only selected
readings, and begin with a singular or plural count. An empty report says
`No included readings`.
