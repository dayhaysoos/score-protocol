# Gauge summary intent

A gauge has a label, numeric amount, suffix, and a `featured` flag. Formatting
uses `Label: amount suffix`. Summaries preserve input order, include only
featured gauges, and begin with a singular or plural count. An empty summary
says `No featured gauges`.
