# Sensor summary intent

A sensor reading has a label, numeric value, unit, and a `featured` flag.
Formatting uses `Label: value unit`. Summaries preserve input order, include
only featured readings, and begin with a singular or plural count. An empty
summary says `No featured readings`.
