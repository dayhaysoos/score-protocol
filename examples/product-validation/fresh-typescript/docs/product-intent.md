# Catalog summary intent

Build a tiny catalog domain with one stable product contract, one reusable
formatter, and one summary function. Prices are integer cents. The formatter
must use a dollar sign and two decimal places. The summary must preserve input
order, include only featured products, and use `No featured products` for an
empty featured selection.
